import { handleChange } from "../src/client/js/changeHandler";

describe("Testing the handleChange function", () => {
  test("The handleChange function should be defined", () => {
    expect(handleChange).toBeDefined();
  });
});
